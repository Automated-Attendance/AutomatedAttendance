language: node_js
node_js:
  - "7"
  
cache:
  bundler: true
  directories:
    - node_modules

env:
  - TRAVIS=true

after success: 
  - cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js